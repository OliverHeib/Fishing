Open and run the file \Fishing\Fishing.pde

This game does not run on macOS as a result of alternate characters being returned when a key is held down (the user would have to repetedly tap the key to move).
